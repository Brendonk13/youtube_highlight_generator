
Next:
    - fix api to work with snackbar
    - make one add video button which has the query
    the video classes shown in a list get start,end, videoId
    - do i need a snackbar for this ?? -- just nice for failures
    - test snackbar
    - get it to show videos
    - get additional buffer of videos
    - add clerk
    
Design:
    - when creating clips:
        show each one vertically in order

    - Download:
        - just generate a bash script to download the videos at the timestamp with youtubedl and ffmpgeg
